[package]
name = "frontend"
version = "0.1.0"
edition = "2024"

[dependencies]
macroquad = "0.4.14"
ureq = "2.10"        # Simple HTTP client (no async runtime needed)
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"   # JSON parsing for events
